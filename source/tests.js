minispade.register('app/tests/poc', function() {
module( "Proof of concept" );

test( "Array length provides the number of an array items", function() {

  equal( ['hi', 'again'].length, 2, 'Length should be 2 when array has 2 items');

});


});